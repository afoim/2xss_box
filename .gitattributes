# 仓库里一律存 LF。开发机是 Windows，而 CI 与 Cloudflare 侧都是 Linux ——
# 让 core.autocrlf 之类的本地配置决定行尾，迟早会在 workflow 的 shell 脚本上
# 炸出 `$'\r': command not found`。
* text=auto eol=lf

# 二进制/产物类，别做任何转换
*.png binary
*.jpg binary
*.woff2 binary

# lockfile 与生成的图标子集：diff 没有可读性，折叠掉
pnpm-lock.yaml linguist-generated=true -diff
src/lib/icons/subset.json linguist-generated=true -diff
