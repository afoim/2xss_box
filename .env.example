# 复制成 .env 后按需填写。Vite 只把 VITE_ 前缀的变量注进前端包。
# 全部可以不填 —— 默认值就是线上正在跑的服务，开箱即用。
#
# 本项目是**纯静态**产物（一个 index.html + 一堆哈希资源），自身不含任何服务端。
# 下面这几个地址都是**已经存在的**远端接口，且都带 Access-Control-Allow-Origin: *，
# 浏览器能直接读，不需要代理、不需要 Worker。

# B 站封面解析（前缀形态，前端拼上 encodeURIComponent(用户输入)）
# 契约：GET <前缀><编码后的输入> → {"pic": "http://i0.hdslb.com/..."}
# VITE_BILI_API=https://bili-pic.2x.nz/?url=

# 文件索引的 JSON 目录树（会读 <base>/index.json，文件也从这个域下载）
# VITE_FILES_BASE_URL=https://raw-files.2x.nz

# Bangumi API 代理（追番列表）。它已经把封面图地址改写成了 lain-bgm-tv.2x.nz，
# 前端拿到就能直接用。
# VITE_BGM_API=https://api-bgm-tv.2x.nz

# 追番读谁的收藏
# VITE_BGM_USER=acofork
